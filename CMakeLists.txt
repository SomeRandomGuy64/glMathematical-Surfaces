# this is the build file for project 
# it is autogenerated by the xmake build system.
# do not edit by hand.

# project
cmake_minimum_required(VERSION 3.15.0)
cmake_policy(SET CMP0091 NEW)
project(glMathematical-Surfaces LANGUAGES CXX C)
set(CURRENT_COMPILER_ID ${CMAKE_CXX_COMPILER_ID})

# target
add_executable(glMathematical-Surfaces "")
set_target_properties(glMathematical-Surfaces PROPERTIES OUTPUT_NAME "glMathematical-Surfaces")
set_target_properties(glMathematical-Surfaces PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/build/linux/x86_64/release")
target_include_directories(glMathematical-Surfaces PRIVATE
    "src/Header Files"
    src/Includes
    src/Shaders
)
target_include_directories(glMathematical-Surfaces SYSTEM PRIVATE
    /home/faizan/.xmake/packages/g/glfw/3.4/4c3c72af70fd4599a3038fc662af9c28/include
    /usr/include/X11/dri
)
target_compile_definitions(glMathematical-Surfaces PRIVATE
    GLFW_INCLUDE_NONE
)
target_compile_options(glMathematical-Surfaces PRIVATE
    $<$<COMPILE_LANGUAGE:C>:-m64>
    $<$<COMPILE_LANGUAGE:CXX>:-m64>
    $<$<COMPILE_LANGUAGE:C>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CXX>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CUDA>:-DNDEBUG>
)
set_target_properties(glMathematical-Surfaces PROPERTIES CXX_EXTENSIONS OFF)
target_compile_features(glMathematical-Surfaces PRIVATE cxx_std_20)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(glMathematical-Surfaces PRIVATE $<$<CONFIG:Release>:-O2>)
else()
    target_compile_options(glMathematical-Surfaces PRIVATE -O3)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
else()
    target_compile_options(glMathematical-Surfaces PRIVATE -fvisibility=hidden)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    set_property(TARGET glMathematical-Surfaces PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
endif()
target_link_libraries(glMathematical-Surfaces PRIVATE
    glfw3
    OpenGL
    Xrandr
    Xinerama
    Xcursor
    Xrender
    X11
    Xi
    Xfixes
    Xext
    xcb
    Xau
    Xdmcp
    GL
    dl
    pthread
)
target_link_directories(glMathematical-Surfaces PRIVATE
    /home/faizan/.xmake/packages/g/glfw/3.4/4c3c72af70fd4599a3038fc662af9c28/lib
)
target_link_options(glMathematical-Surfaces PRIVATE
    -m64
)
# private sourcefiles from sourcebatch c++.build for target glMathematical-Surfaces
target_sources(glMathematical-Surfaces PRIVATE 
    src/main.cpp
    src/camera.cpp
    src/shader.cpp
)
# private sourcefiles from sourcebatch c.build for target glMathematical-Surfaces
target_sources(glMathematical-Surfaces PRIVATE 
    src/glad.c
)

